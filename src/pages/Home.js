import { Typography } from "@mui/material";
import React from "react";

const Home = () => {
    return (
      <Typography variant="h1" component="h1">
        Home
      </Typography>
    );
};

export default Home;
